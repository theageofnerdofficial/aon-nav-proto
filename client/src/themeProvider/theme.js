// Imports:
import themeDefault from './theme_default.json';

// Constants:
const { light, dark } = themeDefault;

/* :
 ************************************************************/
export const lightTheme = {
  // Body:
  body: light.body,
  border: light.border,
};

export const darkTheme = {
  // Body:
  body: dark.body,
  border: dark.border,
};
